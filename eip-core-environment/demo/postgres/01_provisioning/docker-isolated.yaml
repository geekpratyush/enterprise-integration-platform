services:
  postgres:
    image: postgres:latest
    container_name: postgres-platform
    environment:
      POSTGRES_DB: eip_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
