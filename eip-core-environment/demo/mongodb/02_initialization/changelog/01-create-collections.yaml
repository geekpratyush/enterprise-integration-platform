databaseChangeLog:
  - changeSet:
      id: 20260425-01
      author: pratyush
      changes:
        - createCollection:
            collectionName: "operations"
        - createIndex:
            collectionName: "operations"
            keys: "{ 'timestamp': 1 }"
            options: "{ 'name': 'idx_timestamp' }"
        - createCollection:
            collectionName: "business_data"
        - createIndex:
            collectionName: "business_data"
            keys: "{ 'txnId': 1 }"
            options: "{ 'unique': true, 'name': 'idx_txnId_unique' }"
