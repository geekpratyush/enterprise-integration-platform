- route:
    id: "sqlserver-kamelet-insert"
    from:
      uri: "timer:sqlserver-trigger-insert?period=10000&repeatCount=5"
      steps:
        - setHeader:
            name: "txnId"
            simple: "${uuid}"
        - setBody:
            constant: "{\"TXN_ID\": \"SS-SQL-001\", \"STATUS\": \"NEW\", \"PAYLOAD\":
              \"Initial insertion via SQL Server SQL Kamelet\"}"
        - to:
            uri: "kamelet:eip-sql-sink"
            parameters:
              connectionBean: "eip"
              table: "BUSINESS_DATA"
              operation: "insert"
        - wireTap:
            uri: "direct:sqlserver-async-audit"
        - log: ">>> [SQLSERVER-INSERT] Success: ${body}"

- route:
    id: "sqlserver-async-audit"
    from:
      uri: "direct:sqlserver-async-audit"
      steps:
        - to: "kamelet:eip-audit-action?encryption=AES"
        - to:
            uri: "kamelet:eip-sql-sink"
            parameters:
              connectionBean: "eip"
              table: "AUDIT_EVENTS"
        - log: ">>> [SQLSERVER-AUDIT] Asynchronous Audit Captured via Kamelet"

- route:
    id: "sqlserver-kamelet-update"

    from:
      uri: "timer:sqlserver-trigger-update?delay=5000&period=60000&repeatCount=1"
      steps:
        - setBody:
            constant: "[{\"TXN_ID\": \"SS-SQL-001\"}, {\"$set\": {\"STATUS\":
              \"PROCESSED\"}}]"
        - to:
            uri: "kamelet:eip-sql-action"
            parameters:
              connectionBean: "eip"
              table: "BUSINESS_DATA"
              operation: "update"
        - log: ">>> [SQLSERVER-UPDATE] Success: ${body}"

- route:
    id: "sqlserver-kamelet-findall"
    from:
      uri: "timer:sqlserver-trigger-findall?delay=15000&period=60000&repeatCount=1"
      steps:
        - setBody:
            constant: "{}"
        - to:
            uri: "kamelet:eip-sql-action"
            parameters:
              connectionBean: "eip"
              table: "BUSINESS_DATA"
              operation: "findAll"
        - log: ">>> [SQLSERVER-FIND-ALL] Count: ${body.size()}"

- route:
    id: "sqlserver-kamelet-insert-complex"
    from:
      uri: "timer:sqlserver-trigger-insert-complex?delay=25000&period=60000&repeatCount=1"
      steps:
        - setBody:
            constant: >
              {
                "IDENTIFIER": "COMPLEX-001",
                "NOTIFICATIONS": [
                  {"type": "email", "active": true},
                  {"type": "sms", "active": false}
                ],
                "ATTACHMENT": {
                  "$blob": "SGVsbG8sIEJMT0IhIFRoaXMgaXMgYmluYXJ5IGRhdGEu"
                }
              }
        - to:
            uri: "kamelet:eip-sql-sink"
            parameters:
              connectionBean: "eip"
              table: "COMPLEX_DATA"
              operation: "insert"
        - log: ">>> [SQLSERVER-COMPLEX-INSERT] Success: ${body}"

- route:
    id: "sqlserver-kamelet-findall-complex"
    from:
      uri: "timer:sqlserver-trigger-findall-complex?delay=35000&period=60000&repeatCount=1"
      steps:
        - setBody:
            constant: "{}"
        - to:
            uri: "kamelet:eip-sql-action"
            parameters:
              connectionBean: "eip"
              table: "COMPLEX_DATA"
              operation: "findAll"
        - split:
            simple: "${body}"
            steps:
              - log: ">>> [SQLSERVER-COMPLEX-FINDALL] Record: ${body}"
- route:
    id: "sqlserver-kamelet-filter-dynamic"
    from:
      uri: "timer:sqlserver-trigger-filter-dyn?delay=45000&period=60000&repeatCount=1"
      steps:
        - setBody:
            constant: |
              {
                "TXN_ID": {"$like": "SS-SQL-%"},
                "STATUS": ["NEW", "PROCESSED"]
              }
        - to:
            uri: "kamelet:eip-sql-action"
            parameters:
              connectionBean: "eip"
              table: "BUSINESS_DATA"
              operation: "findAll"
        - log: ">>> [SQLSERVER-DYNAMIC-FILTER] Matches: ${body}"
